<%= form_with(model: website, local: true) do |form| %>
  <% if website.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(website.errors.count, "error") %> prohibited this website from being saved:</h2>

      <ul>
      <% website.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_area :url, id: :website_url %>
  </div>

  <div class="field">
    <%= form.label :Dan_id %>
    <%= form.number_field :Dan_id, id: :website_Dan_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
